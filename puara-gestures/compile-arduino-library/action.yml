name: 'compile-arduino-library'
description: 'compiles the different templates to make sure they work'

# TODO: use a build matrix instead of hardcoding the combinatorial explosion.

runs:
  using: "composite"
  steps:
    - name: Download puara-gestures-arduino artifact
      uses: actions/download-artifact@v4
      with:
        name: puara-gestures-arduino

    - name: Install Arduino CLI
      shell: bash
      run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          export PATH="$PWD/bin:$PATH"
          echo "$PWD/bin" >> $GITHUB_PATH
          arduino-cli version
          arduino-cli config init
          arduino-cli core update-index
          find . 

    # Install Board Cores
    - name: Install ESP32 Core
      shell: bash
      run: |
        arduino-cli core install esp32:esp32

    # Install Library Dependencies
    - name: Install Library Dependencies
      shell: bash
      run: |
        arduino-cli lib list
        arduino-cli lib install "Eigen" "boost-embedded-190"
        find .
        mkdir -p $HOME/Arduino/libraries
        ln -s $PWD $HOME/Arduino/libraries/puara
        # Expose Eigen and boost headers inside the puara src tree so transitive
        # includes are resolved without relying on Arduino CLI's auto-detection
        # (which only scans direct sketch includes).
        EIGEN_DIR=$(find $HOME/Arduino/libraries -maxdepth 4 -type d -name "Eigen" \
          ! -path "$HOME/Arduino/libraries/puara/*" 2>/dev/null | head -1)
        echo "Eigen dir: $EIGEN_DIR"
        ln -s "$EIGEN_DIR" $PWD/src/Eigen
        BOOST_DIR=$(find $HOME/Arduino/libraries -maxdepth 4 -type d -name "boost" \
          2>/dev/null | head -1)
        echo "boost dir: $BOOST_DIR"
        ln -s "$BOOST_DIR" $PWD/src/boost

    # Compile Example Sketches for TinyPICO
    - name: Compile for TinyPICO
      shell: bash
      run: |
        arduino-cli compile --fqbn esp32:esp32:um_tinypico examples/utils/blobDetector/blobDetector.ino
